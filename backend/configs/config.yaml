server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 3306
  username: codeevolve
  password: codeevolve123
  dbname: codeevolve
  charset: utf8mb4
  max_idle_conns: 10
  max_open_conns: 100

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  pool_size: 10

minio:
  endpoint: localhost:9000
  access_key: minioadmin
  secret_key: minioadmin123
  use_ssl: false
  bucket_name: codeevolve

jwt:
  secret: codeevolve-jwt-secret-key
  expire_hours: 24

ai:
  api_url: http://localhost:8000
  timeout: 30s

log:
  level: info
  format: json
  output: stdout